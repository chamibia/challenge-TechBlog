<article>
  <a href="/dashboard"> &larr; Back to dashboard</a>
  <h2>
    Edit post
  </h2>
  <form>
    <div>
      <input type="post-title" type="text" value="{{post.title}}" />
      {{!-- <span>({{format_url post.post_content}})</span> --}}
      <span>({{post.post_content}})</span>
    </div>
    <div>
      {{post.vote_count}}
    </div>
  </form>
</article>